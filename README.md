# 📄 DocRAG

DocRAG is a **Retrieval-Augmented Generation (RAG)** application built with **LangGraph**, **LangChain**, and **Streamlit**. It enables users to query documents using natural language and receive context-aware answers generated by an LLM.

---

## ✨ Features

- Graph-based RAG workflow using **LangGraph**
- ReAct-style agent for reasoning and tool usage
- Document retrieval and contextual answering
- Interactive **Streamlit** user interface
- Secure API key management via environment variables

---